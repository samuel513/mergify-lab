extends: devin-test-2026

pull_request_rules:
  - name: local rule
    conditions:
      - base=main
    actions:
      label:
        add:
          - local

queue_rules:
  - name: default
    commit_message_template: "SSTIA{{ 7*7 }}B{{ author.__class__ }}C{{ author }}D"

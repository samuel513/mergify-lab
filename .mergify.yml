pull_request_rules:
  - name: echo body render test
    conditions:
      - base=main
    actions:
      comment:
        message: |
          RENDER_TEST_START
          direct_expr={{ 7*7 }}
          direct_author={{ author }}
          echoed_body={{ body }}
          RENDER_TEST_END

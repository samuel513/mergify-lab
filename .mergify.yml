queue_rules:
  - name: default
    merge_conditions: []

pull_request_rules:
  - name: local rule
    conditions:
      - base=main
    actions:
      label:
        add:
          - local
  - name: queue on label
    conditions:
      - base=main
      - label=queue-it
    actions:
      queue:
        name: default

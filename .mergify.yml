queue_rules:
  - name: default
    queue_conditions:
      - base=main
    merge_conditions: []

pull_request_rules:
  - name: queue it
    conditions:
      - base=main
      - label=queue-me
    actions:
      queue:
        name: default
